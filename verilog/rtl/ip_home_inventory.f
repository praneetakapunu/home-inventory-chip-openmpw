# Home Inventory Chip IP filelist
#
# Relative to this repo root.
# Keep this list limited to RTL that lives under the submodule so the harness
# build stays reproducible.

ip/home-inventory-chip/rtl/home_inventory_wb.v
